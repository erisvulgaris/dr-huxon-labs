# QA Log — Dr. Huxon Labs

## Viewport Profiles
- **Mobile (390px × 844px):** Primary target viewport. Floating glass bottom navigation, cards, drawers, and quizzes are optimized here.
- **Desktop/Laptop (1200px+):** Centered app layout. Left admin sidebar transitions dynamically to top-nav overlay on smaller viewports.

---

## Verified User Journeys
1. **Purchase Path:** Product selection -> add-to-cart toast -> cart drawer checkout validation -> address/OTP entries -> simulation screen. (Pass)
2. **Settings Configurations:** Appearance theme changes, toggle social proof notifications, adjust toast repeat intervals. (Pass)
3. **Admin Dashboard Drill-Down:** KPI widgets load correctly, dynamic CSV export works, audit log tracks mock system logs. (Pass)
